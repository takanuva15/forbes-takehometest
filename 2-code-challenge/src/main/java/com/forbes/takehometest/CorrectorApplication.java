package com.forbes.takehometest;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Run the main method here to bootstrap the web server and make the endpoints publicly available.
 */
@SpringBootApplication
public class CorrectorApplication {

	public static void main(String[] args) {
		SpringApplication.run(CorrectorApplication.class, args);
	}

}
